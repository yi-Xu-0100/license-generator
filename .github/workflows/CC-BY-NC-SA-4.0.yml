name: CC-BY-NC-SA-4.0

on:
  schedule:
    - cron: '30 1 * * *'

jobs:
  run:
    name: Generate CC-BY-NC-SA-4.0 License
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2.3.4

      - name: Generate License
        uses: ./
        with:
          path: LICENSE
          type: CC-BY-NC-SA-4.0

      - name: Show License
        run: |
          cat LICENSE
